{
  "$schema": "../../../schemas/dataform/form-definition.schema.json",
  "id": "spawn_rules_header",
  "title": "Spawn Rules Header",
  "description": "Defines the header fields for a spawn_rules document.",
  "summarizer": {
    "phrases": [
      {
        "visibility": [{ "field": "identifier", "comparison": "defined" }],
        "tokens": [
          { "type": "literal", "text": "identifies as " },
          { "type": "value", "field": "identifier", "humanify": "minecraft" }
        ]
      }
    ]
  },
  "fields": [
    {
      "id": "identifier",
      "title": "Id",
      "dataType": 2
    },
    {
      "id": "population_control",
      "title": "Population group",
      "description": "Specifies a group that this mob is connected with, of which only so many mobs in the group can be spawned",
      "dataType": 2
    }
  ]
}
