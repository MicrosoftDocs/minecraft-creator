{
  "$schema": "../../../schemas/dataform/form-definition.schema.json",
  "summarizer": {
    "phrases": [
      {
        "tokens": [
          {
            "type": "literal",
            "text": "checks if entity is rolling"
          }
        ]
      }
    ]
  },
  "id": "query.is_rolling",
  "fields": [],
  "description": "Returns 1.0 if the entity is rolling, else it returns 0.0. Applies generally to the panda.",
  "generated_doNotEdit": {
    "id": "query.is_rolling",
    "title": "Is Rolling",
    "description": "Returns 1.0 if the entity is rolling, else it returns 0.0.",
    "fields": []
  }
}